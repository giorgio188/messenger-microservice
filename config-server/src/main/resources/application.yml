server:
  port: 8888  # ????, ?? ??????? ????? ???????? Config Server

spring:
  application:
    name: config-server  # ??? ???????
  cloud:
    config:
      server:
        git:
          uri: https://github.com/your-repo/config-repo  # Git-??????????? ? ??????????????
          search-paths: configs  # ????? ? ???????????, ??? ???????? ????????????